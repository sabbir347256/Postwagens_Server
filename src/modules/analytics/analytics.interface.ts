export type TPageViews = {
  label: string;
  value: number;
};

export type TUserGrowth = {
  label: string;
  percentage: number;
};

export type TTopListing = {
  listingTitle: string;
  views: number;
  inquiries: number;
  conversionRate: number;
};
